# /src/save — manual save / load

Serializes the whole World to JSON. Save happens ONLY when the user clicks Save.
There is NO autosave and no background persistence of any kind.

Planned (added in later phases): IndexedDB persistence via localforage,
export/import of a downloadable `.json`, and scenario presets.
